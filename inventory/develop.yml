all:
  children:
    manager:
      hosts:
        manager01:
          hostvars_sample: "test1"
      vars:
        groupvars_sample1: "gvar1"
        groupvars_sample2: "gvar2"
  vars:
    docker_control_user: "ec2-user"
